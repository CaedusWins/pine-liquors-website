# Instructions for Claude

Before doing anything else in this repo, read **`PROJECT_CONTEXT.md`** —
it's the maintained handoff doc covering business details, design
decisions, repo/CI setup, local machine tooling, and open TODOs. Treat it
as current-state truth, not just history.

**Keep it updated.** Whenever a change in this session is significant
enough that a future session (or a human pasting the file into a fresh
chat) would need to know about it — a new decision, a structural change,
a resolved or newly discovered TODO, a tradeoff made — update
`PROJECT_CONTEXT.md` in the same commit as the change, not as an
afterthought.
